import type { Dashboard } from './dashboard';

/** The token prompt, and the verifying state while the first status read is in flight. */
export default function LoginPage({ token, error, signOut, setError, sessionEpoch, setToken, draftToken, setDraftToken }: Pick<Dashboard, 'token' | 'error' | 'signOut' | 'setError' | 'sessionEpoch'> & { setToken(token: string): void; draftToken: string; setDraftToken(value: string): void }) {
  return <main className="login"><div className="brand"><img className="mark" src="/graphyard-symbol.svg" alt="" width="32" height="32"/> graphyard</div><h1>Keep the work<br/>moving forward.</h1><p>One place for ownership, evidence, and delivery.</p>{error && <p role="alert" className="notice danger">{error}</p>}{token ? <><p role="status">Verifying connection…</p><button onClick={signOut}>Use another token</button></> : <form onSubmit={e => { e.preventDefault(); const value = draftToken.trim(); if (!value) { setError('Enter an access token.'); return; } sessionEpoch.current++; setError(''); sessionStorage.setItem('graphyard-token', value); setToken(value); }}><label>Access token<input type="password" required value={draftToken} onChange={e => setDraftToken(e.target.value)} autoComplete="off" placeholder="Your Graphyard token"/></label><button>Open control plane ↗</button></form>}<small>Tokens are scoped to your role and kept for this browser session.</small></main>;
}
