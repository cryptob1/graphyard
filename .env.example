DATABASE_URL=postgres://graphyard:graphyard@localhost:5438/graphyard
PORT=4310
HOST=127.0.0.1
# Use distinct random secrets, at least 32 characters. Never share admin tokens with agents.
# A producer's "proofs" list is a bootstrap seed only: Graphyard materializes it into a grant
# record on first startup, and proof authority is managed with "graphyard grants" from then on.
GRAPHYARD_PRINCIPALS=[{"id":"operator","role":"admin","token":"replace-with-a-long-random-admin-secret"},{"id":"master","role":"coordinator","token":"replace-with-a-long-random-coordinator-secret"},{"id":"agent-1","role":"worker","token":"replace-with-a-long-random-worker-secret"},{"id":"ci","role":"producer","proofs":["integration:claim-safety"],"token":"replace-with-a-long-random-ci-secret"}]
# Exact repository-relative paths the regression guard treats as generated rather than owned,
# comma separated. The installer adapters derive it from the managed repository's manifest
# (node scripts/check-docs.mjs --list); unset, no file is exempt and a value that does not parse
# refuses start-up.
GRAPHYARD_GENERATED_FILES=
# One GitHub repository per control plane in v0.1.
GITHUB_REPOSITORY=your-org/your-repo
GITHUB_BASE_BRANCH=main
GITHUB_APP_ID=
GITHUB_INSTALLATION_ID=
GITHUB_PRIVATE_KEY_FILE=
GITHUB_WEBHOOK_SECRET=
# IDs of apps allowed to supply CI checks (verify for your installation).
GITHUB_CI_APP_IDS=15368
# Reviewer GitHub App identities for the agent review provider. Numeric identities only,
# never private keys; each must be distinct from GITHUB_APP_ID. See examples/reviewer-apps.json.
GRAPHYARD_REVIEWER_APPS=[]
