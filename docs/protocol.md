<!-- page: Build integrations | 1 | roles, commands, leases, evidence, webhook. -->
<!-- generated by scripts/check-docs.mjs; do not edit: run npm run docs:check -- --write -->
# Agent protocol and HTTP API

For an integration author: which protocol page answers the call you are about to make.

- **Authentication:** every endpoint except `/healthz` and the HMAC-verified webhook requires `Authorization: Bearer TOKEN`; API credentials are not Git credentials
- **Transport:** HTTPS for remote machines

One topic per page; this index is generated in full.

## Agent protocol

1. [Roles, requests and reads](protocol/roles.md) — authentication and retries.
2. [Work commands](protocol/work-commands.md) — every mutation.
3. [Leases, supervision and workspaces](protocol/leases.md) — leases, supervision, workspaces.
4. [Read endpoints](protocol/read-endpoints.md) — which GET returns what.
5. [Evidence and proof authority](protocol/evidence.md) — evidence, grants, revocation.
6. [GitHub webhook and review providers](protocol/github-webhook.md) — webhooks and review providers.
7. [Bootstrap mode for a change that introduces its own proof harness](protocol/bootstrap-mode.md) — deferring a proof.
8. [Merge-queue bindings and carry](protocol/merge-queue-binding.md) — base binding and carry.
9. [Pipeline timeline](protocol/pipeline-speed.md) — the per-item timeline.
