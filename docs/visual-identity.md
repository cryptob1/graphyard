<!-- page: Understand or contribute | 4 | marks, palette, voice. -->
# Graphyard visual identity

For anyone placing the mark, and what colour may never mean.

The mark draws a lowercase **g** as a connected path, its three nodes independently owned work joining a delivery flow.

Assets:

- `web/public/graphyard-symbol.svg` — pale green, for dark backgrounds.
- `web/public/graphyard-symbol-ink.svg` — dark green, for light backgrounds.
- `web/public/favicon.svg` — the simplified mark on a dark rounded square, for browser tabs and small sizes.

Usage:

- All three are self-contained SVGs under the repository's Apache-2.0 licence; edit the path coordinates directly.
- Keep clear space of one node diameter.
- Never stretch or rotate the mark.

Palette — decorative, never a status indicator; status uses text and accessible UI semantics:

- pale green `#c5e69b`
- ink `#243923`
- dark surface `#111714`
