# Fake test fixtures only: every line is a gitleaks fingerprint of a finding in a test file,
# never a real secret and never a src/ or configuration path. Prefer writing a fixture as an
# obvious non-secret (docs/onboarding.md); record a fingerprint only when a fixture must keep a
# realistic shape to do its job.
# GY-409: fake API-key fixture in a test, not a real credential.
31d6c0f2b6e173b2dfaaaf7ad473ceebe665de2a:tests/connect-account.test.ts:generic-api-key:25
