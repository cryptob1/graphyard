import { test, expect } from '@playwright/test'; test('private-title-marker', async () => { console.log('private-stdout-marker'); await test.step('private-step-marker', async () => { expect(1).toBe(1); }); });